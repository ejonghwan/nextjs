import React from 'react';

const login = () => {
    return (
        <div>
            <form>
                <input type="text" />
                <input type="password" />
                <button type="submit">submit</button>
            </form>
        </div>
    );
};

export default login;